# CacheSec SLS uses libfreenect for Kinect v1 RGB/IR/depth.
# The generic Linux gspca Kinect webcam driver claims the camera interface and
# blocks libfreenect depth capture, so keep it unloaded on this host.
blacklist gspca_kinect
